{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import json,sys,subprocess; d=json.load(sys.stdin); p=d.get('tool_input',{}).get('file_path',''); subprocess.run(['rustfmt', p]) if p and p.endswith('.rs') else None\""
          },
          {
            "type": "command",
            "command": "python3 -c \"import json,sys,subprocess; d=json.load(sys.stdin); p=d.get('tool_input',{}).get('file_path',''); subprocess.run(['ruff', 'check', '--fix', p]) if p and p.endswith('.py') else None\""
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(cargo clippy *)",
      "Bash(cargo tarpaulin *)",
      "Bash(cargo test)",
      "Bash(cargo test *)",
      "Bash(git check-ignore *)",
      "Bash(git fetch *)",
      "Bash(make coverage)",
      "Bash(make test)",
      "Bash(make test-hooks)",
      "mcp__plugin_context-mode_context-mode__ctx_batch_execute",
      "mcp__plugin_context-mode_context-mode__ctx_execute",
      "mcp__plugin_context-mode_context-mode__ctx_fetch_and_index"
    ]
  }
}
