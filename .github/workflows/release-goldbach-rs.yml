name: release-goldbach-rs

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version number without the v prefix (e.g. 1.2.0)"
        required: true
        type: string

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: write
  id-token: write

jobs:
  release:
    name: Release goldbach-rs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: goldbach/goldbach-rs

      - name: Run tests
        run: make test
        working-directory: goldbach/goldbach-rs

      - name: Build release binary
        run: cargo build --release
        working-directory: goldbach/goldbach-rs

      - name: Generate CHANGELOG
        uses: orhun/git-cliff-action@f50e11560dce63f7c33227798f90b924471a88b5  # v4
        with:
          config: cliff.toml
          args: --verbose --include-path "goldbach/**" --tag-pattern "goldbach-v.*"
        env:
          OUTPUT: goldbach/goldbach-rs/CHANGELOG.md
          GITHUB_REPO: ${{ github.repository }}

      - name: Generate release notes (latest only)
        uses: orhun/git-cliff-action@f50e11560dce63f7c33227798f90b924471a88b5  # v4
        with:
          config: cliff.toml
          args: --latest --strip all --include-path "goldbach/**" --tag-pattern "goldbach-v.*"
        env:
          OUTPUT: /tmp/release-notes.md
          GITHUB_REPO: ${{ github.repository }}

      - name: Commit CHANGELOG
        continue-on-error: true
        env:
          VERSION: ${{ inputs.version }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add goldbach/goldbach-rs/CHANGELOG.md
          git diff --staged --quiet && exit 0
          git commit -m "chore(changelog): update goldbach for v${VERSION}"
          git push

      - name: Generate SHA256 checksum
        run: |
          (cd goldbach/goldbach-rs/target/release && sha256sum goldbach) > goldbach/goldbach-rs/target/release/goldbach.sha256
          cat goldbach/goldbach-rs/target/release/goldbach.sha256

      - name: Create and push tag
        env:
          VERSION: ${{ inputs.version }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag "goldbach-v${VERSION}"
          git push origin "goldbach-v${VERSION}"

      - name: Create GitHub release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: "goldbach-v${{ inputs.version }}"
          name: "goldbach v${{ inputs.version }}"
          body_path: /tmp/release-notes.md
          files: |
            goldbach/goldbach-rs/target/release/goldbach
            goldbach/goldbach-rs/target/release/goldbach.sha256

  sign:
    needs: [release]
    uses: ./.github/workflows/release-sign.yml
    with:
      release_tag: "goldbach-v${{ inputs.version }}"
      binary_name: "goldbach"
    permissions:
      id-token: write
      contents: write
