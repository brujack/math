name: release-collatz-rs

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version number without the v prefix (e.g. 1.2.0)"
        required: true
        type: string

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: write
  id-token: write

jobs:
  release:
    name: Release collatz-rs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c  # stable

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: collatz/collatz-rs

      - name: Run tests
        run: make test
        working-directory: collatz/collatz-rs

      - name: Build release binary
        run: cargo build --release
        working-directory: collatz/collatz-rs

      - name: Generate CHANGELOG
        uses: orhun/git-cliff-action@f50e11560dce63f7c33227798f90b924471a88b5  # v4
        with:
          config: cliff.toml
          args: --verbose --include-path "collatz/**" --tag-pattern "collatz-v.*"
        env:
          OUTPUT: collatz/collatz-rs/CHANGELOG.md
          GITHUB_REPO: ${{ github.repository }}

      - name: Generate release notes (latest only)
        uses: orhun/git-cliff-action@f50e11560dce63f7c33227798f90b924471a88b5  # v4
        with:
          config: cliff.toml
          args: --latest --strip all --include-path "collatz/**" --tag-pattern "collatz-v.*"
        env:
          OUTPUT: /tmp/release-notes.md
          GITHUB_REPO: ${{ github.repository }}

      - name: Commit CHANGELOG
        continue-on-error: true
        env:
          VERSION: ${{ inputs.version }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add collatz/collatz-rs/CHANGELOG.md
          git diff --staged --quiet && exit 0
          git commit -m "chore(changelog): update collatz for v${VERSION}"
          git push

      - name: Generate SHA256 checksum
        run: |
          (cd collatz/collatz-rs/target/release && sha256sum collatz) > collatz/collatz-rs/target/release/collatz.sha256
          cat collatz/collatz-rs/target/release/collatz.sha256

      - name: Create and push tag
        env:
          VERSION: ${{ inputs.version }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag "collatz-v${VERSION}"
          git push origin "collatz-v${VERSION}"

      - name: Create GitHub release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: "collatz-v${{ inputs.version }}"
          name: "collatz v${{ inputs.version }}"
          body_path: /tmp/release-notes.md
          files: |
            collatz/collatz-rs/target/release/collatz
            collatz/collatz-rs/target/release/collatz.sha256

  sign:
    needs: [release]
    uses: ./.github/workflows/release-sign.yml
    with:
      release_tag: "collatz-v${{ inputs.version }}"
      binary_name: "collatz"
    permissions:
      id-token: write
      contents: write
