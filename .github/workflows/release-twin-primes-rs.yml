name: release-twin-primes-rs

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version number without the v prefix (e.g. 1.2.0)"
        required: true
        type: string

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: write
  id-token: write

jobs:
  release:
    name: Release twin-primes-rs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: twin-primes/twin-primes-rs

      - name: Run tests
        run: make test
        working-directory: twin-primes/twin-primes-rs

      - name: Build release binary
        run: cargo build --release
        working-directory: twin-primes/twin-primes-rs

      - name: Generate CHANGELOG
        uses: orhun/git-cliff-action@f50e11560dce63f7c33227798f90b924471a88b5  # v4
        with:
          config: cliff.toml
          args: --verbose --include-path "twin-primes/**" --tag-pattern "twin-primes-v.*"
        env:
          OUTPUT: twin-primes/twin-primes-rs/CHANGELOG.md
          GITHUB_REPO: ${{ github.repository }}

      - name: Generate release notes (latest only)
        uses: orhun/git-cliff-action@f50e11560dce63f7c33227798f90b924471a88b5  # v4
        with:
          config: cliff.toml
          args: --latest --strip all --include-path "twin-primes/**" --tag-pattern "twin-primes-v.*"
        env:
          OUTPUT: /tmp/release-notes.md
          GITHUB_REPO: ${{ github.repository }}

      - name: Commit CHANGELOG
        continue-on-error: true
        env:
          VERSION: ${{ inputs.version }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add twin-primes/twin-primes-rs/CHANGELOG.md
          git diff --staged --quiet && exit 0
          git commit -m "chore(changelog): update twin-primes for v${VERSION}"
          git push

      - name: Generate SHA256 checksum
        run: |
          (cd twin-primes/twin-primes-rs/target/release && sha256sum twin-primes) > twin-primes/twin-primes-rs/target/release/twin-primes.sha256
          cat twin-primes/twin-primes-rs/target/release/twin-primes.sha256

      - name: Create and push tag
        env:
          VERSION: ${{ inputs.version }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag "twin-primes-v${VERSION}"
          git push origin "twin-primes-v${VERSION}"

      - name: Create GitHub release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: "twin-primes-v${{ inputs.version }}"
          name: "twin-primes v${{ inputs.version }}"
          body_path: /tmp/release-notes.md
          files: |
            twin-primes/twin-primes-rs/target/release/twin-primes
            twin-primes/twin-primes-rs/target/release/twin-primes.sha256

  sign:
    needs: [release]
    uses: ./.github/workflows/release-sign.yml
    with:
      release_tag: "twin-primes-v${{ inputs.version }}"
      binary_name: "twin-primes"
    permissions:
      id-token: write
      contents: write
